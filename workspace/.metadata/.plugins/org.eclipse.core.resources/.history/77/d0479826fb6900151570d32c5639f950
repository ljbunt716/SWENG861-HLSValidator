package validators;

import hlsfiles.HLSFileWDirectory;
import validators.Common.CaseValidator;

public class FileHasEXTAsUpperValidator extends Validator {

	public FileHasEXTAsUpperValidator(HLSFileWDirectory file) {
		File = file;
	}

	public void Validate() {
		if (!CaseValidator.IsValidCase(File.HLSText, "#EXT"))
			super.AddValidationError("All mentions of EXT in file are not Uppercase : ",
					"File Has EXT all Uppercase Validation");
	}
}
